 {# {% extends 'base.html.twig' %} 

 {% block title %}Brands{% endblock %}  #}

{% block body %}

<!-- Modal -->
<div class="modal fade" id="afficheModal" tabindex="-1" aria-labelledby="exampleModalLabel" aria-hidden="true">
  <div class="modal-dialog">
    <div class="modal-content">
      <div class="modal-header">
        <h5 class="modal-title" id="exampleModalLabel">Modéle</h5>
        <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close">x</button>
      </div>
      <div class="modal-body">
        <p>{{ model.model }} </p>
      </div>
      <div class="modal-footer">
        <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Close</button>
       <a href="{{ path('model_index') }}" class="btn btn-primary">Retour à la liste</a>
         <a href="{{ path('model_edit', {'id': model.id}) }}" class="btn btn-primary">Modifier</a>
      </div>
    </div>
  </div>
</div>



    {# <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ brand.id }}</td>
            </tr>
            <tr>
                <th>Marque</th>
                <td>{{ brand.brand }}</td>
            </tr>
        </tbody>
    </table> #}

   
{# 
    {{ include('brands/_delete_form.html.twig') }} #}
{% endblock %}
