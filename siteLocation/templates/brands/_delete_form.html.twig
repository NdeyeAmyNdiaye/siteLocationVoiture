<form method="post" action="{{ path('brands_delete', {'id': brand.id}) }}" onsubmit="return confirm('Etes-vous sur de vouloir supprimer cette marque');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ brand.id) }}">
    <button class="btn btn-danger">Supprimer</button>
</form>
